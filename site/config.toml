baseurl = "https://lainradio.club/" # Controls base URL
languageCode = "en-UK" # Controls site language
title = "Lain Radio Club" # Homepage title and page title suffix
paginate = 11 # Number of posts to show before paginating
publishDir = "../docs"

theme = "after-dark" # Set default theme

enableRobotsTXT = true # Suggested, enable robots.txt file
googleAnalytics = "" # Optional, add tracking Id for analytics
disqusShortname = "" # Optional, add Disqus shortname for comments
SectionPagesMenu = "main" # Enable menu system for lazy bloggers
footnoteReturnLinkContents = "↩" # Provides a nicer footnote return link

[params]
  description = "" # Suggested, controls default description meta
  author = "zeta@pleroma.soykaf.com" # Optional, controls author name display on posts
  hide_author = false # Optional, set true to hide author name on posts
  show_menu = true # Optional, set true to enable section menu
  powered_by = true # Optional, set false to disable credits
  made_by = true
  images = [] # Suggested, controls default Open Graph images
  theme_variant = "" # Optional, for use to overriding default theme

[params.seo]
  disable_bpg = true # Disable BPG image support
  
[[menu.main]]
  name = "Home"
  weight = 1
  identifier = "home"
  url = "/"
[[menu.main]]
  name = "Events"
  weight = 2
  identifier = "event"
  url = "/event/"
[[menu.main]]
  name = "Posts"
  weight = 3
  identifier = "post"
  url = "/post/"
